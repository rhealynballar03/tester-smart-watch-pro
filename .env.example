# Kie.ai API key for image generation (scripts/kie_image.py).
# Get one from your dashboard at https://kie.ai, then copy this file to .env
# and fill in the value. .env is gitignored and never committed.
KIE_API_KEY=your_kie_api_key_here
